enum guiPath {
  home = "/",
  login = "/login",
  logout = "/logout",
  signup = "/signup",
  users = "/users",
  message = "/message",
  messages = "/messages",
  chat = '/chat'
}

export default guiPath;
