enum guiPath {
  home = "/",
  login = "/login",
  logout = "/logout",
  signup = "/signup",
  users = "/users",
  message = "/message",
  messages = "/messages",
  chats = "/chats",
}

export default guiPath;
