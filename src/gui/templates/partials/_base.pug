doctype html
html 
    head
        block head
            meta(charset="UTF-8")
            meta(name="viewport" content="width=device-width, initial-scale=1.0")

            link(rel="stylesheet", href="https://fonts.googleapis.com/css?family=Lato:300,300i,700")
            title Morze chat | #{title}
            
    body(data-alert=`${alert ? alert : ''}`)
        include _header

        block content 

        include _footer