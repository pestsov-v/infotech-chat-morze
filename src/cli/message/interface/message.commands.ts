enum messageCommands {
  sendMessage = "send message to concreticate user",
  listMessages = "list all messages on database",
  userMessages = "List all messages on concreticate user",
  decodeMessage = "decode concreticate message",
  backToMainMenu = "back to main menu",
}

export default messageCommands;
